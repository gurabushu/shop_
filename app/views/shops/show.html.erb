<h2>店舗詳細</h2>

<div class="shop-show-card">
  <h3><%= @shop.title %></h3>
  <p><%= @shop.description %></p>
  <p>カテゴリー：<%= @shop.category %></p>
  <p>
    営業時間：
    <%= @shop.open_time&.strftime("%H:%M") %> 〜 <%= @shop.close_time&.strftime("%H:%M") %>
  </p>
</div>

<div class="reservation-wrapper">
<%= link_to "予約する", new_shop_reservation_path(@shop), class: "action-btn" %>
</div>

<%= link_to "戻る", shops_path, class: "back-link" %>